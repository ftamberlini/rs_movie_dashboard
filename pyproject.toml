[project]
name = "rs-movie-dashboard"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.138.0",
    "oracledb>=4.0.1",
    "python-dotenv>=1.2.2",
]
